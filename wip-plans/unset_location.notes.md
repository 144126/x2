- 2026-08-14 live pass on prod, all four flows green: profile set GH/AF stuck across reload; clear -> stored co:'' st:'' and the form no longer re-proposes the ip guess; room edit set then clear drops country+state from the room view and the about panel; /app/user/<id> stops printing the COUNTRY and STATE rows. agent-browser trap: refs go stale on every page change and a stale ref clicks whatever now holds it — that cost one wrong 'edit modal is broken' diagnosis.
